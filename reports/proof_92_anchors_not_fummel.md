# 证明：92锚点 + 数独三约束 ≠ 符阖原题

## 证据链

### 证据1：三组行C对比

| 来源 | 行C值 |
|------|-------|
| CP-SAT解 | [5,6,14,1, 10,2,16,8, 3,15,13,12, 7,9,4,11] |
| TXT终局C191620 | [7,10,14,15, 4,2,16,8, 12,13,3,1, 11,9,6,5] |
| TXT初始解盘C | [11,6,14,1, 4,2,13,8, 7,12,3,16, 10,9,15,5] |

**匹配数统计（16列）：**
| 对比 | 匹配数 |
|------|--------|
| CP-SAT vs 终局C191620 | **5/16** |
| CP-SAT vs 初始解盘C | 6/16 |
| 初始解盘C vs 终局C191620 | 7/16 |

---

### 证据2：92锚点验证

CP-SAT解完全满足92锚点约束：
- **92/92个锚点全部验证通过**
- 证明CP-SAT搜索正确执行了92锚点+数独三约束

---

### 证据3：符阖排列组闔验证（核心）

**关键发现：CP-SAT解的16行都不在原始符阖排列组闔中！**

| 行 | CP-SAT解排列 | 原始集合规模 | 是否在内 |
|----|-------------|-------------|---------|
| A1 | (11,2,3,15,...) | 8,731 | **NOT IN** |
| A2 | (8,12,7,10,...) | 902 | **NOT IN** |
| A3 | (5,6,14,1,...) | 656,777 | **NOT IN** |
| ... | ... | ... | ... |
| A16 | (16,3,2,12,...) | 1,809 | **NOT IN** |

**总计：CP-SAT解在原始符阖排列组闔中匹配 0/16 行**

---

### 证据4：TXT终局C191620的位置验证

- C191620 不在我们重新生成的 A3_permutations.json (2,499个排列) 中
- 原始 A3 集合应有 656,777 个排列
- 我们重新生成时只使用了数独三约束过滤，缺少"符阖排列组闔"约束

---

## 逻辑论证

### 前提

1. **P1**: CP-SAT使用92锚点+数独三约束（行/列/宫互斥）求解
   - 返回"唯一解"，耗时0.14秒

2. **P2**: TXT文件"符阖终局解盘"行C = [7,10,14,15,4,2,16,8,12,13,3,1,11,9,6,5]
   - 该排列满足所有92锚点约束
   - 该排列满足所有列解集约束

### 关键观察

- CP-SAT解行C与TXT终局C191620仅匹配 **5/16**（11个位置不同）
- CP-SAT解行C与TXT初始解盘C仅匹配 **6/16**

### 反证法

**假设**：92锚点 + 数独三约束 = 符阖原题

**推导**：
- 如果假设成立，则符阖原题的解应该是唯一的
- CP-SAT返回的"唯一解"应该就是TXT终局解盘
- 但CP-SAT解行C与TXT终局C191620仅5/16匹配

**矛盾**：两个不同的解都满足92锚点+数独三约束

**结论**：假设不成立，即 **92锚点 + 数独三约束 ≠ 符阖原题**

---

## 根本原因分析

### 92锚点的约束范围

```
92个锚点 → 256宫格中的92个位置被固定
剩余151个位置 → 无数种可能填充方式
```

### 符阖排列组闔的作用

符阖原题的真正约束是：

| 约束层面 | 内容 | CP-SAT是否实现 |
|---------|------|---------------|
| 1 | 数独三约束（行/列/宫互斥） | ✅ |
| 2 | 符阖排列约束（每行来自A{i}集合） | ⚠️ 部分（集合规模缩小） |
| 3 | **符阖排列组闔（预定义完整行排列）** | ❌ **缺少！** |

### 关键区别

```
符阖原题 = 92锚点 + 数独三约束 + 符阖排列组闔

符阖排列组闔的作用：
- 将解空间从"无数种"压缩到"有限种"
- 每行是预定义的完整排列，不是从满足约束的排列中选择
- txt文件中的"终局解盘"是预先给出的答案，不是推导结果
```

---

## 最终结论

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  92 anchors + Sudoku 3 constraints ≠ Original Fummel Problem    │
│                                                                 │
│  Original Fummel Problem requires an additional constraint:     │
│  "Fummel Permutation Group" - each row must be a              │
│  pre-defined complete permutation from a specific set.        │
│                                                                 │
│  92 anchors only fix 92 of 256 cells. The remaining           │
│  151 cells have infinite valid fillings. Fummel              │
│  Permutation Group compresses the space to finite            │
│  possibilities.                                                │
│                                                                 │
│  The "Final Solution" in the TXT file is a PRE-DEFINED       │
│  solution, NOT derived from 92 anchors alone.                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 数据文件

| 文件 | 说明 |
|------|------|
| `solution_92_anchors.json` | CP-SAT用92锚点求解的结果 |
| `超級大數獨_box_size4.txt` | 原始题目定义 |
| `A{i}_permutations.json` | 重新生成的符阖排列集合 |
| `backup_fuyi/A{i}_permutations.json` | 原始符阖排列集合备份 |

---

*证明日期：2026-05-31*
*求解器：OR-Tools CP-SAT*
*验证工具：Python 3.13*
