# 符阖排列 16x16 数独 - DLX 冲突分析报告

## 执行结果

| 任务 | 结果 |
|------|------|
| 解的数量 | ❌ **0**（无解） |
| 单源值总数 | 92 个 |
| 冲突单源值 | 0 个（每个单源值都有排列支持） |
| 搜索时间 | <1 秒 |

## 关键发现

### 1. 约束集不可满足

DLX 搜索确认 **不存在任何解**。这不是因为"概率太低"，而是因为约束集本身存在内在冲突。

### 2. 92 个单源值形成"锁定链"

每列有 5-10 个值只能从特定行来：

```
Col 12: 10 个单源值（最严重）
  值 2 ← Row 10（只能从 Row10 来）
  值 3 ← Row 13
  值 4 ← Row 2
  值 5 ← Row 5
  值 6 ← Row 8
  值 7 ← Row 14
  值 9 ← Row 4
  值 10 ← Row 11
  值 11 ← Row 12
  值 15 ← Row 6

Col 5: 8 个单源值
Col 7: 7 个单源值
Col 14: 7 个单源值
```

### 3. 锁定链冲突机制

```
锁定链示例：
Row 10 必须提供 Col 12 的值 2
Row 10 必须提供 Col 5 的值 5（也是单源值）
...
Row 10 只有 28,984 个排列，但需要满足多个单源值约束

如果 Row 10 的排列无法同时满足所有"必须从 Row 10 来的值"，
就会产生冲突。
```

### 4. 各行单源值负载

| 行 | 排列数 | 单源值数 | 负载率 |
|----|--------|----------|--------|
| Row 9 | 164 | 7 | 4.3% |
| Row 6 | 359 | 6 | 1.7% |
| Row 12 | 620 | 6 | 1.0% |
| Row 2 | 902 | 8 | 0.9% |
| Row 13 | 484 | 7 | 1.4% |
| 其他行 | >1000 | 3-6 | <1% |

**Row 9 负载最高**：164 个排列中有 7 个单源值约束（4.3%）

## 冲突根源分析

### 为什么 92 个单源值导致无解？

```
根本原因：
1. 每列的 16 个值必须来自 16 个不同行
2. 但 92 个单源值强制某些值必须从特定行来
3. 如果某行被强制提供太多值，超出其排列能覆盖的范围 → 冲突

数学条件：
对于每行 r，设 S_r = {单源值分配给行 r 的数量}
设 P_r = {行 r 的排列数}

如果存在行 r 使得：
    |{v | (c, v) 是单源值且 r 是唯一来源}| > P_r 的有效覆盖数
则无解。
```

### 具体冲突检测

检查每行的单源值能否被排列覆盖：

```python
for row in 1..16:
    ss_count = 单源值分配给该行的数量
    valid_perms = 排列中能同时满足所有单源值的数量
    
    if valid_perms == 0:
        冲突！
```

结果：所有行的单源值都有排列支持，但**组合后仍然无解**。

这意味着冲突是**全局性的**——不是单个行的问题，而是多行之间的相互作用。

## 重新提取排列（去除锁定冲突）

### 方法

基于已知数字重新提取排列，保留符合固定值的排列：

| 行 | 原始排列 | 重新提取后 |
|----|----------|------------|
| Row 3 | 407,669 | 24,342 |
| Row 5 | 633,271 | 36,352 |
| 其他行 | 基本不变 | 基本不变 |

### 结果

- 单源值数量仍为 92 个
- 约束冲突依然存在

**结论**：重新提取无法消除根本冲突，因为单源值是全局约束的结果。

## 解决方案建议

### 方案 1: 移除部分单源值约束

```
识别"过度锁定"的行：
- Row 9: 7 个单源值 / 164 排列 → 移除部分
- Row 6: 6 个单源值 / 359 排列

策略：对每行最多允许 N 个单源值
```

### 方案 2: 重新生成排列集

```
基于标准数独解生成符阖排列：
1. 先生成一个标准数独解
2. 提取每行的排列
3. 确保列/宫约束满足
4. 从这些排列中构建符阖排列集
```

### 方案 3: 调整已知数字

```
减少固定值数量或调整位置：
- 当前 55 个固定值可能导致排列集过窄
- 减少到 40-45 个可能恢复可解性
```

## 代码交付

| 文件 | 内容 |
|------|------|
| `dlx_analysis.py` | DLX 计数与冲突分析脚本 |
| `dlx_result.json` | 分析结果数据 |
| `conflict_detailed_report.md` | 本报告 |
